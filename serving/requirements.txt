# LOCAL live-serving tier dependencies (tier 3).
# CPU-only by default; the heuristic backend needs nothing beyond the core stack.
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27           # APIReasoningLLM (hosted-LLM backend) + TestClient
pydantic>=2.0
pandas>=2.0
numpy>=1.24
pyarrow>=14.0         # read the crash-signal Parquet store
streamlit>=1.30       # dashboard.py

# LSTM volatility endpoint (CPU wheel is fine for serving).
torch>=2.0
scikit-learn>=1.3

# OPTIONAL — only for SERVING_BACKEND=finetuned (the Kaggle adapter):
# transformers>=4.40
# accelerate>=0.30
# peft>=0.10
